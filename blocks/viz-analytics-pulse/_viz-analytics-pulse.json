{
  "definitions": [
    {
      "title": "Viz Analytics Pulse",
      "id": "viz-analytics-pulse",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Viz Analytics Pulse",
              "model": "viz-analytics-pulse"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "viz-analytics-pulse",
      "fields": [
        {
          "component": "text",
          "name": "dataUrl",
          "label": "Data URL",
          "valueType": "string",
          "description": "Analytics endpoint returning JSON with activeUsers, pageviewsToday, topPages, byRegion"
        },
        {
          "component": "text",
          "name": "refreshSeconds",
          "label": "Refresh Interval (seconds)",
          "valueType": "string",
          "description": "How often to poll the data endpoint (default: 30)"
        },
        {
          "component": "text-area",
          "name": "inlineData",
          "label": "Inline Data",
          "valueType": "string",
          "description": "JSON fallback analytics snapshot"
        },
        {
          "component": "text",
          "name": "accentColor",
          "label": "Accent Color",
          "valueType": "string",
          "description": "Highlight color for counters and charts (hex or name, default: #0066ff)"
        }
      ]
    }
  ],
  "filters": []
}
